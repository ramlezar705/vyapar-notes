[phases.setup]
nixPkgs = ['python312', 'gcc']

[phases.install]
cmds = ['python -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt']

[start]
cmd = '. /opt/venv/bin/activate && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8080}'
